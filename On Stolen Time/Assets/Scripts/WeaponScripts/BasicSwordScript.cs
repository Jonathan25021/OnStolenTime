using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicSwordScript : MeleeWeaponScript
{
    // Start is called before the first frame update
    void Start()
    {
        _damage = 30;
        _attackSpeed = 10f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
